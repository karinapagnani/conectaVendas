<!DOCTYPE html>
<html>
    <head>
        <title>Sistema de Gerenciamento de Usuários</title>
    </head>
  

        <h2>Atualizar Usuários</h2>

        <form action="update" method="post">
            <input type="text" placeholder="Id"
                name="id">
            <input type="text" placeholder="Nome"
                name="nome">
            <input type="text" placeholder="Email"
                name="email">
            <input type="text" placeholder="Idade"
                name="idade">
            <button type="submit">Atualizar</button>
        </form>


<style>

           /* Cor de fundo para o corpo da página */
           body {
                background-color: #ffffff; /* Um tom suave de cinza */
                font-family: Arial, sans-serif;
                margin: 0;
                padding: 0;
            }

            /* Estilos da tabela */
            table {
                font-family: Arial, sans-serif;
                border-collapse: collapse;
                width: 100%;
                background-color: #ffffff; /* Fundo branco para a tabela */
            }

            td,
            th {
                border: 1px solid #dddddd;
                text-align: left;
                padding: 8px;
            }

            tr:nth-child(even) {
                background-color: #f2f2f2; /* Cor de fundo alternada para linhas da tabela */
            }

            tr:nth-child(odd) {
                background-color: #ffffff; /* Cor de fundo para linhas ímpares da tabela */
            }

            /* Botões */
            button {
                background-color: #1a3dda; /* Cor verde para os botões */
                color: white;
                padding: 10px 15px;
                border: none;
                cursor: pointer;
                margin: 5px;
            }

            button:hover {
                background-color: #727272; /* Cor verde mais escura quando o botão é hover */
            }

            /* Título */
            h2 {
                text-align: center;
                color: #333;
                margin-top: 20px;
            }

        

    
    </style>

    <body>
        <h2>Sistema de Gerenciamento de Usuários</h2>
        <table>
            <tr>
                <th>Id</th>
                <th>Nome</th>
                <th>Email</th>
                <th>Idade</th>
                <th>Excluir</th>
            </tr>
            <% data.forEach(element => { %>
            <tr>
                <td>
                    <%= element.id %>
                </td>
                <td><%= element.nome %></td>
                <td><%= element.email %></td>
                <td><%= element.idade %></td>
                <td>
                    <form
                        action="/delete"
                        method="post"
                    >
                        <input
                            type="text"
                            style="display: none"
                            name="id"
                            value="<%= element.id %>"
                        />
                        <button type="submit">
                            Deletar
                        </button>
                    </form>
                </td>
            </tr>
            <% }) %>
        </table>


    </body>
</html>
